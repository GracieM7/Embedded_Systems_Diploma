#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {

	char arr[100];

	int size;

	printf("Enter a string:");
	fflush(stdout);
	fgets(arr, strlen(arr), stdin);
	size=strlen(arr);

	for(int i=size-1;i>-1;i--){
		printf("%c",arr[i]);


	}


    return 0;
}
