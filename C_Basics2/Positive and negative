#include <stdio.h>
#include <stdlib.h>

int main(void) {
	float a;
	printf("Enter a number:\n");
	scanf("%f",&a);
	if(a>0)
		printf("%f is positive",a);
	else if (a<0)
		printf("%f is negative",a);
	else
		printf("%f is 0",a);
	return EXIT_SUCCESS;
}
